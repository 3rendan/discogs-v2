<nav class="navbar navbar-expand-sm navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'Finance Tracker', root_path, class: 'navbar-brand' %>
    <% if current_user %>
    <div class="container">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item <%= 'active' if request.path == my_portfolio_path %>">
          <%= link_to 'My Portfolio', my_portfolio_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if request.path == my_friends_path %>">
          <%= link_to 'Friends', my_friends_path, class: 'nav-link' %>
        </li>
        <%= link_to edit_user_registration_path, class: "nav-link" do %>
        <%= fa_icon 'edit' %>   Edit Profile
        <% end %>
      </ul>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item user-name">
          <%= fa_icon 'user' %>
          <%= current_user.full_name %>
        </li>
        <li class="nav-item">
          <%= link_to('Logout', destroy_user_session_path,
              class:'btn btn-xs btn-danger logout-btn', method: :delete) %>
        </li>
        </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
    </div>
    <% end %>
  </div>
</nav>
